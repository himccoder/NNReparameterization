# requirements.txt
# ─────────────────────────────────────────────────────────────────────────────
# Dependencies for the structural optimization ablation study.
# Install with: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# Core scientific computing
numpy>=1.21.0
scipy>=1.7.0

# Automatic differentiation (for physics gradients)
# Note: use the 'autograd' package, NOT 'torch.autograd'
autograd>=1.4

# Classical structural optimization solver (MMA)
nlopt>=2.7.0

# Deep learning framework (for NN parameterizations)
torch>=1.12.0

# Visualization
matplotlib>=3.5.0

# Data analysis and results aggregation
pandas>=1.4.0

# Image processing (for problem setup utilities)
scikit-image>=0.19.0
